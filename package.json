{
  "name": "facl-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:visual:navbar": "playwright test tests/navbar-visual.spec.js tests/navbar-states.spec.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "vite": "^5.0.0"
  }
}
